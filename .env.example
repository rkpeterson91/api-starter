NODE_ENV=development
PORT=3000

# Database Configuration
# Update DB_USER with your PostgreSQL username (often your system username)
# Leave DB_PASSWORD empty if you're using local PostgreSQL without a password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=api_starter_db
DB_USER=your_username_here
DB_PASSWORD=
