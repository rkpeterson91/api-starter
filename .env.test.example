# Test Environment Configuration
# Copy this to .env.test if you want to override the defaults in vitest.config.ts
# By default, tests use: DB_USER=postgres, DB_PASSWORD=postgres, DB_NAME=api_starter_db (with _test appended by database.cjs)

DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=api_starter_db
